#include <stdio.h>
#include <stdlib.h>

void LeerVector (float *vector, int n);
float ProductoEscalar (float *v1, float *v2, int n);
void MostrarVector (float *v, int n);

int main(int argc, char **argv) {
    int n;
    int j;
    float *v1;
    float *v2;
    
    j=0;
    
    do{
    do{
        printf("indique el tamano del vector: ");
        scanf("%d",&n);
        if(n<1){
            printf(" la dimension del vector tiene que ser mayor a 0");
        }
    }while(n<1);
    
    v1= (float*) malloc(n * sizeof(float));
    if(v1==NULL){
      printf("error al asignar a la memoria"); }
        else{
            v2= (float*) malloc(n * sizeof(float));
                if(v2==NULL){
                printf("error al asignar a la memoria"); }
                    else{
            LeerVector( v1, n);
            LeerVector( v2, n);
            MostrarVector( v1, n);
            MostrarVector( v2, n);
            printf("el producto escalar del verctor uno y dos es  %f",ProductoEscalar( v1, v2, n));
            
            free (v2);
        }
            free (v1);
        }
        printf("desea repertir la prueba, si la respuesta es si, pulse 1 ");
        fflush(stdin);
        scanf("%d",&j);
        
    }while(j !=1 );
    printf("fin de programa");
    return 0;
}

void LeerVector(float *vector, int n){
    int i;
    for(i=0; i<n; i++){
        printf("introduce el valor de la posicion v[%d]",i+1);
        scanf("%f", vector[i]);
    }    
}

float ProductoEscalar (float *v1, float *v2, int n){
    int i;
    float elemento;
    float res;
    res=0;
    for(i=0;i<n;i++){
    elemento = v1[i]*v2[i];
    res=res+elemento;
        }
    return res;
}

void MostrarVector (float *v, int n){
int i;
    for(i=0;i<n;i++){
    printf(" %g ", *(v+i));
    }
}
